data:
  content_dir: debug_data/content # Thư mục chứa ảnh cấu trúc (ảnh gốc cần biến đổi)
  style_dir: debug_data/style     # Thư mục chứa ảnh phong cách (texture/màu sắc muốn học)
  image_size: 256
  batch_size: 3
  num_workers: 4
  val_split: 0.1

train:
  epochs: 30
  lr: 0.0002
  grad_clip: 1.0
  mixed_precision: true
  ema_decay: 0.9999
  log_every: 10
  save_every: 5
  checkpoint_dir: checkpoints
  seed: 42

loss_weights:
  noise: 1.0      # Trọng số cho hàm loss chính của DDPM
  style: 0.5      # Trọng số ép mô hình học phong cách
  content: 0.1    # Trọng số ép mô hình giữ lại cấu trúc ban đầu

early_stopping:
  patience: 15
  min_delta: 0.0001